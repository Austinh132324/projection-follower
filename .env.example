# Copy to .env and adjust. Only needed for the optional "real mode" backend
# (local SQLite persistence + ESPN proxy). The static GitHub Pages app needs none of this.

# Where the SQLite DB lives. Gitignored.
PF_DATA_DIR=./data

# API server port (the dashboard talks to this in real mode).
PF_PORT=4000
